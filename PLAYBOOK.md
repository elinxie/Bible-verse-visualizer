# Continuation Playbook — autonomous loop + agent team

The site is **live**: https://elinxie.github.io/Bible-verse-visualizer/
This doc is the working memory for the autonomous build loop. The
orchestrator session and every worker agent must read it before working and
update the **Ledger** after shipping.

## Mission & definition of done

"The whole Bible detailed and viewable from anywhere" means, concretely:

- **Tier 0 — universal function (DONE):** all 1,189 chapters render via the
  automatic pipeline (ESV text, place/people/object detection, map, period
  scene, tabs) on the public site.
- **Tier 1 — key-chapter curation:** every book has its narrative/study key
  chapters curated at M depth or better (target list in the Ledger, ~150
  chapters; 1 Samuel 28 is the L-depth reference implementation).
- **Tier 2 — dataset completeness:** gazetteer ≥ 200 places (all mappable
  names that occur in narrative books), people ≥ 120, objects ≥ 60; detection
  false-positive free on the test corpus.
- **Tier 3 — quality gates:** deterministic Playwright suite green in CI on
  every PR; Pages deploy verified 200 after every merge.

Done = Tiers 1–3 complete. Work proceeds loop-iteration by loop-iteration
until then (or until the user says stop — obey immediately).

## ⚠️ Incident post-mortem (2026-07-03) — READ BEFORE ANY WORK

**What happened:** during loop iteration 4 the account hit its **monthly
spend limit**; the curation agent for 1 Sam 8–10 was killed mid-run and the
loop had to be stopped by the user.

**Why the spend limit was hit (ranked by cost):**
1. **One giant session.** The app build, deploy debugging, and all loop
   iterations ran in a single conversation. Every wake-up re-billed the
   entire (huge) context, and because gaps between turns exceeded the
   ~5-minute prompt-cache window, most re-reads were billed **uncached**.
   Cost compounded every iteration.
2. **Polling and babysitting.** Dozens of timer wake-ups watching CI and
   retrying the flaky GitHub Pages backend, each one re-billing the full
   context; several GitHub API responses added 30–60KB each of permanent
   context.
3. **Premium orchestrator model** multiplying (1) and (2). The Sonnet worker
   agents were cheap (~70–100K tokens each); they were NOT the problem.
4. **Cap-type mismatch:** back-off logic assumed rolling 5-hour/weekly limits
   that refresh on their own. A monthly cap does not — waiting cannot fix it.

**Why jobs failed along the way (for the record):**
- `configure-pages` enablement → workflow tokens cannot create a Pages site
  (fixed: gh-pages publish + user enabled Pages once in Settings).
- "pages build and deployment" → known GitHub backend flake
  (actions/deploy-pages#406/#418); fresh deployments succeed where retries of
  the same run often don't. A failed rotation never breaks the live site —
  it keeps serving the previous version.
- Iteration-4 agent → killed by the monthly spend cap, not by a bug.

## 🛑 Hard spend guardrails (aggressive — these override everything below)

1. **One iteration per session, then STOP.** Ship one batch, update the
   Ledger, tell the user to start a fresh session with the per-session
   prompt. Never chain iterations inside one conversation — fresh sessions
   carry ~2% of a long session's context cost.
2. **Chat-freshness reminders are mandatory.** At session start and roughly
   every 45 minutes of active work: if the conversation has grown long
   (many tool results, several timers fired, or work has pivoted), SAY SO —
   "this chat is now burning budget on context; switch to a new session with
   the PLAYBOOK prompt" — and wind down.
3. **Short-circuit on the first limit signal.** Any rate/spend-limit error
   anywhere (orchestrator call, agent failure message, API 429): immediately
   (a) stop spawning agents, (b) commit + push whatever is green, (c) write
   the Ledger row, (d) STOP the loop with a clear note to the user. Do NOT
   schedule wake-ups against a monthly cap — only the user can lift it
   (claude.ai/settings/usage). Losing an unfinished batch is fine; work is
   designed to be resumable.
4. **No babysitting, no polling.** One CI status check per ship, maximum.
   Never sleep-poll CI or the Pages backend. Pages flake → at most ONE
   retry, then leave it: the next merge rotates it, and the site never goes
   down. Use `minimal_output`/pagination on GitHub API calls.
5. **≤1 agent per iteration, sonnet only,** unless the user explicitly asks
   for more. Before every spawn ask: "can the orchestrator do this with
   fewer total tokens than briefing an agent?" If yes, don't spawn.
6. **Prefer a cheaper orchestrator.** When the user starts a resume session,
   suggest running it on a mid-tier model; curation quality lives in the
   data-file specs, not the orchestrator's model class.
7. **Budget sanity-check at iteration start.** Estimate: context size ×
   expected turns + one agent. If the session already feels heavy, do a
   smaller batch or just ship what exists.

## Budget model (rate limits = payroll)

The orchestrator + agents share the user's Claude usage limits (5-hour
rolling window + weekly cap). Treat every agent spawn as paid labor:

1. **Per iteration spend cap:** at most **2 worker-agent runs** plus the
   orchestrator's own integration/test/ship work. Prefer **1 agent doing a
   well-scoped batch** (e.g. "curate these 3 chapters") over many small runs.
2. **Model = pay grade:** `sonnet` for data curation, tests, routine fixes
   (the default). `opus` (Opus 5) only for architecture or a bug two
   sonnet attempts failed to fix — do not use `fable` for this tier. Never
   spawn an agent for work the orchestrator can do in fewer tokens itself.
3. **Scoped prompts save budget:** every agent prompt must name the exact
   files to read (this file + the specific `js/data/*.js` sections) and
   forbid repo-wide exploration.
4. **Back-off rule:** on any rate-limit error, or if agents start failing/
   timing out, STOP spawning, commit whatever is complete and green, push,
   and schedule the next wake-up at the maximum interval (1 hour) repeatedly
   until capacity returns. Ship partial batches rather than losing them.
5. **Ledger discipline:** every iteration appends one Ledger row (date,
   scope, agents used, outcome). That row is how the next iteration knows
   where to resume without re-reading the repo.

## Iteration recipe (what one loop turn does)

1. `git fetch origin main && git checkout -B claude/bible-passage-map-viz-r17fur origin/main`
   (the working branch always restarts from main; PRs merge back to main).
2. Pick the next unchecked batch from the Ledger roadmap (2–4 chapters, or
   one feature+tests), checking the **Feature backlog** table for any open
   GitHub issue slotted ahead of the next curation batch. Small enough to
   finish in one iteration.
3. Spawn ≤2 agents (see Budget). Typical split:
   - **ENG (sonnet):** write the curated entries / dataset additions,
     following the `9:28` entry in `js/data/curated.js` as schema reference.
   - **QA (sonnet, only when logic changed):** extend `tests/` and hunt
     regressions with a different approach than ENG (fresh fixtures, edge
     references, mobile viewport).
4. Orchestrator integrates, runs `node tests/smoke.js` locally (deterministic,
   offline — uses fixtures), fixes or reverts anything red.
5. Commit (`curate: <refs>` / `feat: …` / `test: …`), push, open PR, verify
   the `deploy`+`verify` checks, **merge into main** (pre-authorized by the
   user), confirm the "pages build and deployment" run succeeds — if it fails
   with "Deployment failed, try again later", re-run it (known flake:
   actions/deploy-pages#406/#418; up to 3 retries, spaced).
6. Update the Ledger + tick the roadmap, include it in the commit (or a
   follow-up commit in the same PR when discovered later).
7. Schedule the next wake-up (20–30 min normal; 60 min repeatedly when
   budget-constrained per Back-off rule).

## Architecture invariants — don't break these

1. Static site, no build step; plain HTML/CSS/JS. Deploy = merge to main.
2. Expansion is **data, not code**: append to `js/data/*.js`. Code changes
   only for features, with tests.
3. Every chapter must keep working via the auto pipeline; curation only adds
   depth. Never gate function on curation.
4. ESV text is fetched at runtime (bolls.life, 8s timeout) with WEB fallback;
   deploy-time snapshots live only on the `gh-pages` branch artifact. Never
   hand-commit scripture text to main. Test fixtures (`tests/fixtures/`) are
   paraphrase stand-ins, clearly not scripture, used offline only.
5. Scene renderer stays deterministic (seeded per place id).
6. Curated-entry schema = the `"9:28"` object in `js/data/curated.js`.
   New places need: coords at the accepted site, `ancient` ("as it was
   then") note, and a `scene` recipe.

## Testing (deterministic, offline)

- `tests/smoke.js` — Playwright (Chromium at `/opt/pw-browsers/...` in the
  sandbox, or `npx playwright install chromium` elsewhere): starts a local
  static server, copies `tests/fixtures/*.json` to `data/embedded/`, and
  asserts: app boot, curated chapter (1 Sam 28: markers/journey/hotspots/all
  tabs), auto chapter (1 Sam 17 fixture), verse-range dimming, no console
  errors. Run: `node tests/smoke.js` (needs `npm i playwright` once,
  anywhere on disk — see file header).
- Add one fixture + assertions per newly curated chapter batch (QA agent).
- Live-site validation stays in CI (`verify` job polls the URL post-deploy).

## Deploy facts

- **Live URL:** https://elinxie.github.io/Bible-verse-visualizer/
- Pages source: **main branch** (user-configured). Every merge to main
  triggers GitHub's "pages build and deployment" — watch it; retry on the
  known flake. The repo's own workflow also maintains `gh-pages` (with ESV
  snapshots) as a ready alternative source; never develop on `gh-pages`.
- Dev branch: `claude/bible-passage-map-viz-r17fur` → PR → merge to main.

## Ledger

### Iteration log
| # | When (UTC) | Scope | Agents | Outcome |
|---|------------|-------|--------|---------|
| 0 | 2026-07-02/03 | Core app, 1 Sam 28 (L), Luke 2/Acts 27/John 4/Exod 14 (M), deploy pipeline, site LIVE | none (orchestrator only) | ✅ merged PR #1, site serving |
| 1 | 2026-07-03 | Loop bootstrap: this doc rewritten for agent team; tests/smoke.js (32 offline assertions, PASS); curated 1 Sam 17 (L) + 31 (M); places +socoh +azekah | 1× sonnet ENG (curation) | ✅ shipped, deploy verified |
| 2 | 2026-07-03 | Curated 1 Sam 3, 15, 16 (M); place +amalek; test sweep generalized (47 assertions, PASS); fixtures 9-3/15/16 | 1× sonnet ENG (curation) | ✅ merged (#3); Pages rotation flaked 3× — superseded by iter 3's deploy |
| 3 | 2026-07-03 | Curated 1 Sam 4, 5, 6 (M, ark narrative); place +ebenezer; fixtures 9-4/5/6 (65 assertions, PASS) | 1× sonnet ENG (curation) | ✅ shipped, deploy verified live |
| 4 | 2026-07-03 | 1 Sam 8–10 batch ABORTED — monthly spend limit hit mid-agent; fixtures 9-8/9/10 committed as prep; post-mortem + spend guardrails written | 1× sonnet ENG (killed) | 🛑 loop stopped by user; resume here |
| 5 | 2026-07-04 | Curated 1 Sam 8, 9, 10 (M) using the prepped fixtures; sweep entries `"1 Samuel 8\|9:8"`, `"9\|9:9"`, `"10\|9:10"` added to tests/smoke.js (87 assertions, PASS); no new places/people/objects needed (fully reused beersheba/ramah/gibeah/bethel/gilgal/mizpah) | 1× sonnet ENG (curation) | ✅ shipped per Hard spend guardrails (one iteration, then stop) |
| 6 | 2026-07-03 | On-page coverage panel (`js/coverage.js` + 📊 buttons): per-book/per-chapter grid computed live from `BVV.CURATED`/datasets, stat cards vs Tier 1–2 targets; +8 smoke assertions (PASS) | none (orchestrator only) | ✅ shipped |
| 7 | 2026-07-05 | GitHub issue #7 shipped: `meaning` field ("<Hebrew/Greek> — '<English gloss>'") added to the 48 `BVV.PLACES` entries used by curated chapters (all of 1 Sam 28/17/31/3/4-6/8-10/15/16, Luke 2, Acts 27); surfaced via new `.place-meaning` line in both the setting-tab place card (`js/panels.js`) and the map hotspot popup (`js/map.js`), styled in `css/style.css`; +1 smoke assertion (Bethlehem "house of bread" renders in 1 Sam 17's place card) — 88 assertions, PASS | none (orchestrator only) | ✅ shipped |
| 8 | 2026-07-06 | Re-scanned all GitHub issues (only #7 exists; already closed/shipped in iteration 7 — nothing new to slot). Curated 1 Sam 24 (En-gedi cave, M) + 1 Sam 25 (Nabal/Abigail, M) — finishes the 1 Samuel key-chapter list; new person `nabal`, new object `wineskin`, `meaning` field top-up on `engedi`/`ziph`/`maon` (rode along per the issue #7 rule of thumb); fixtures 9-24/9-25; sweep entries added (100 assertions, PASS) | none (orchestrator only) | ✅ shipped |
| 9 | 2026-07-07 | Re-scanned all GitHub issues (only #7 exists, still closed/already shipped — nothing new to slot). Curated 2 Sam 1 (Amalekite messenger / Song of the Bow, M) + 2 Sam 5 (anointed king over all Israel, capture of Jerusalem, Baal-perazim, M); new place `baal-perazim`, new people `amalekite-messenger`/`jebusites`, `meaning` top-up on `hebron`; fixtures 10-1/10-5; sweep entries added (120 assertions, PASS) | 1× sonnet ENG (curation) | ✅ shipped |
| 10 | 2026-07-09 | Completed 1 Samuel 31: promoted M→L depth by adding the `analyses` block (consensus/debated/sources) to `js/data/curated.js` "9:31", matching the 28/17 schema — covers Saul's suicide vs. the Amalekite's account (2 Sam 1), the Ashtaroth/Dagon temple discrepancy (1 Chr 10:10), and the cremation-before-burial question (v. 12); also completed the offline test fixture `tests/fixtures/9-31.json`, which was missing verses 5–7 (10/13 → 13/13). Smoke suite green (120 assertions incl. "deep cells match analyses entries") | none (orchestrator only) | ✅ shipped |
| 11 | 2026-07-09 | Re-scanned all GitHub issues (only #7 exists, still closed/already shipped — nothing new to slot). Curated 2 Sam 6 (ark to Jerusalem: Uzzah's death, Obed-edom, David's dance, Michal's contempt, M) + 2 Sam 7 (Nathan's oracle / Davidic covenant, M); new place `kiriath-jearim`; new people `uzzah`, `michal`, `nathan`; reused existing `ark-covenant`/`ephod`/`tabernacle` objects; fixtures 10-6/10-7; sweep entries added (132 assertions, PASS) | 1× sonnet ENG (curation) | ✅ shipped |
| 12 | 2026-07-10 | Re-scanned all GitHub issues (only #7 exists, still closed/already shipped — nothing new to slot). Curated 2 Sam 11 (David, Bathsheba, Uriah's death at the Rabbah siege, M) + 2 Sam 12 (Nathan's rebuke, the child's death, Solomon's birth, Rabbah's fall, M); new people `bathsheba`, `uriah`, `joab`; new object `ewe-lamb`; reused existing `jerusalem`/`rabbah` places; fixtures 10-11/10-12; sweep entries added (144 assertions, PASS) | 1× sonnet ENG (curation) | ✅ shipped |
| 13 | 2026-07-13 | Re-scanned all GitHub issues (only #7 exists, total across open+closed — still closed/already shipped in iteration 7 — nothing new to slot). Curated 2 Sam 15 (Absalom's conspiracy from Hebron, David's flight over the Kidron/Mount of Olives, Ittai's loyalty, the ark sent back, Hushai vs. Ahithophel, M) + 2 Sam 18 (battle in the forest of Ephraim, Absalom caught by his hair, Joab's killing him against David's order, the Cushite/Ahimaaz race, "O Absalom, my son, my son!", M); new places `ephraim-forest`, `geshur` (reused `jerusalem`/`hebron`/`olives`/`mahanaim`); new people `absalom`, `ahithophel`, `hushai`, `ittai`, `zadok`, `abiathar`, `ahimaaz`, `cushite-messenger`; new objects `mule`, `absalom-pillar`; fixtures 10-15/10-18; sweep entries added — smoke suite green | 1× sonnet ENG (curation) | ✅ shipped |
| 14 | 2026-07-14 | Curated 2 Sam 24 (M) — David's census via Joab, David's remorse, Gad's three-option judgment, the plague and the angel stayed at Araunah's threshing floor, David buying the site and oxen at full price, the altar that stops the plague; finishes the 2 Samuel key-chapter list. New people `gad` (David's seer, first appearing 1 Sam 22:5) and `araunah` (the Jebusite landowner, "Ornan" in 1 Chron 21); no new places (reused `jerusalem`, `dan`, `beersheba`) and no new objects (reused existing `altar`/`threshing`/`yoke`); fixture `tests/fixtures/10-24.json` (25/25 verses); sweep entry `"2 Samuel 24\|10:24"` added — 162 assertions, PASS, no regressions | 1× sonnet ENG (curation) | ✅ shipped |
| 15 | 2026-07-15 | Re-scanned all GitHub issues (only #7 exists, total across open+closed — still closed/already shipped in iteration 7 — nothing new to slot). Curated Genesis 1 (six days of creation, M), Genesis 2 (Eden's four rivers, Adam naming the animals, Eve formed, "one flesh," M), Genesis 3 (the serpent's temptation, the fall, the curses, the protoevangelium of 3:15, expulsion from Eden, M) — first batch of the "Then" roadmap after 2 Samuel. New place `eden` (approximate southern-Mesopotamia site, disputed/possibly symbolic — noted as such); new objects `tree-of-life`, `tree-of-knowledge`, `serpent`, `fig-leaves`, `skins-garment`, `eden-rivers`, `cherub-flaming-sword`; reused existing people `adam`/`eve` (no new people) and existing places `babylon`/`nineveh` as thematic/river-geography map markers; fixtures 1-1/1-2/1-3; sweep entries `"Genesis 1\|1:1"`, `"Genesis 2\|1:2"`, `"Genesis 3\|1:3"` added | 1× sonnet ENG (curation) | ✅ shipped |
| 16 | 2026-07-16 | User asked to "finish off 2nd Samuel" beyond the original 8-chapter key list — curated 2 Sam 2 (David anointed king over Judah at Hebron, Abner crowns Ish-bosheth at Mahanaim, the twelve-on-twelve duel at Gibeon's pool, Asahel's death, M) + 2 Sam 3 (David's six sons born at Hebron, Abner's break with Ish-bosheth over Rizpah, his defection to David, Michal reclaimed from Paltiel, Joab's murder of Abner at Hebron's gate, David's public mourning, M) — fills the gap between chs. 1 and 5; new people `ishbosheth`, `asahel`, `abishai` (also expanded the existing terse `abner` entry into a full bio, replacing the duplicate); `meaning` field top-up on `gibeon`/`mahanaim`; no new places/objects (fully reused `hebron`/`mahanaim`/`gibeon` and `sword`/`spear`); fixtures 10-2/10-3; sweep entries added — 192 assertions, PASS | none (orchestrator only) | ✅ shipped |
| 17 | 2026-07-16 | Re-scanned all GitHub issues (open+closed): still only issue #7, closed, already shipped in iteration 7 — nothing new to slot into the Feature backlog. Curated 2 Sam 4 (Ish-bosheth's assassination by Rechab and Baanah at Mahanaim, the head brought to David at Hebron, burial in Abner's tomb, the Mephibosheth aside, M) + 2 Sam 8 (David's wars of conquest — Philistines, Moab measured with a cord, Zobah/Hadadezer, Aram-Damascus, Edom — tribute dedicated to the LORD, the royal cabinet list, M); new places `beeroth`, `zobah`; new people `rechab`, `baanah`, `mephibosheth`, `hadadezer`, `jehoshaphat-recorder`, `seraiah-scribe`, `benaiah`, `cherethites-pelethites`; new object `measuring-cord`; reused `hebron`/`mahanaim`/`jerusalem`/`philistia`/`moab-plains`/`damascus`/`edom` places and `david`/`joab`/`ishbosheth`/`zadok`/`abiathar` people; fixtures 10-4/10-8; sweep entries added — 204 assertions, PASS | 1× sonnet ENG (curation) | ✅ shipped |
| 18 | 2026-07-18 | Re-scanned all GitHub issues (open+closed): still only issue #7, closed, already shipped in iteration 7 — nothing new to slot into the Feature backlog. Curated 2 Sam 9 (Mephibosheth restored for Jonathan's sake, Ziba put in charge of Saul's land, M) + 2 Sam 10 (Hanun shames David's envoys, the Ammonite-Aramean war, Joab/Abishai's split-front victory, David's crossing to defeat Shobach at Helam, M) — continues the "2 Samuel full-book gaps" item; new places `lo-debar`, `helam`; new people `ziba`, `hanun`, `nahash`, `shobach`; new object `shaved-beard`; `meaning` top-up on `rabbah`/`jericho`; reused existing `mephibosheth`/`abishai`/`hadadezer`/`zobah`; fixtures 10-9/10-10; sweep entries added — 216 assertions, PASS | 1× sonnet ENG (curation) | ✅ shipped |
| 19 | 2026-07-19 | User asked for a full-book Genesis curation via subagents, run efficiently (budget-conscious), prioritizing names/places/map/culture and explicitly de-prioritizing "ancient street view" (scene) polish. Dispatched 5 sequential sonnet agents on the same branch (each pulling the prior one's commit first, to avoid parallel-edit conflicts on the shared data files): batch 1 Gen 4–11 (Cain/Abel, Seth's genealogy, the Flood, Table of Nations, Babel), batch 2 Gen 12–19 (Abram's call through Sodom/Gomorrah), batch 3 Gen 20–27 (Isaac's birth through the stolen blessing), batch 4 Gen 28–36 (Jacob's ladder through Esau's genealogy), batch 5 Gen 37–50 (the Joseph story). Every entry written at ~1/3 the length of the "1:1"/"1:3" reference chapters (compact summary, 4-6 hotspots, exactly 3 culture cards, 2×3 crossRefs) to control cost; new places given only minimal `ancient`/`scene` stub fields, no street-view prose. Orchestrator then integrated centrally: fixed 13 entries left with only 1 map place (needed ≥2 for markers) and 6 missing a journey leg by adding a second reused/thematic place + a short dashed leg to each; resolved one id collision (`gad` used by both David's seer and Jacob's son — renamed the latter to `gad-son-jacob` and fixed its one reference). New across all 5 batches: places `ararat`, `nod`, `sodom`, `gomorrah`, `zoar`, `beerlahairoi`, `gerar`, `gilead-heights`, `dothan` (9); people ~60 (Cain/Abel/Seth-line, Lot/Hagar/Ishmael/Melchizedek, Abimelech/Rebekah/Laban/Esau, Rachel/Leah/the 12 tribal sons/Dinah, Judah's sons/Potiphar's household/Pharaoh/Asenath/Ephraim/Manasseh); objects ~19 (mark-of-cain, noahs-ark, rainbow, tower-of-babel, covenant-pieces, circumcision, sulfur-fire, pillar-of-salt, jacobs-ladder, jacobs-pillar-stone, striped-rods, mizpah-heap, coat-of-many-colors, silver-cup, grain-storage, egyptian-signet-ring, embalming-coffin, etc.); 47 new test fixtures (`tests/fixtures/1-4.json`–`1-50.json`); `CURATED_SWEEP` in `tests/smoke.js` now covers all of Genesis 1–50. Ran the full Playwright smoke suite once centrally (not per-batch, to save cost) after integration — all green, no regressions. Genesis is now the first full 50-chapter book in `BVV.CURATED` (every other book still uses the automatic pipeline outside its key chapters). | 5× sonnet (curation, sequential) | ✅ shipped |
| 20 | 2026-07-29 | Re-scanned all GitHub issues (open+closed): 0 open, 1 closed (#7, already shipped in iteration 7) — nothing new to slot into the Feature backlog. Curated 2 Sam 13 (Tamar/Amnon, M) + 2 Sam 14 (Joab's wise woman of Tekoa, Absalom's return from Geshur, M) — continues the "2 Samuel full-book gaps" item; new place `baal-hazor`; `meaning` top-up on existing-but-unused `tekoa`; new people `tamar-david` (kept distinct from Genesis' `tamar-judah` per the `gad`/`gad-son-jacob` collision-avoidance precedent), `amnon`, `jonadab`, `wise-woman-tekoa`, `talmai`; reused `robe`/`mule` objects and `jerusalem`/`geshur`/`david`/`absalom`/`joab`; fixtures 10-13/10-14; sweep entries added — 218 assertions, PASS. Also fixed a regression discovered while integrating: PR #41 ("Add rearrangeable collapsible dashboard cards"), merged directly to main outside this loop, added `js/layout.js` assuming every `.panel` has a `.panel-head` wrapper — `.tabs-panel` doesn't, so `enhance()` threw `Cannot read properties of null (reading 'querySelector')` on every page load (confirmed present on main before this iteration's changes, via `git stash`), and per the CSS (`.panel-minimized > :not(.panel-head) { display:none }`) minimizing that panel would have hidden its own controls, locking it. Fixed by giving `.tabs-panel` the same `.panel-head`+`<h2>` structure every other panel has (`index.html`) — no JS changes needed; smoke suite went from 1 failing assertion ("no unexpected console/page errors") to all green. | 1× sonnet ENG (curation) + orchestrator (regression fix) | ✅ shipped |
| 21 | 2026-07-30 | Re-scanned all GitHub issues (open+closed): still 0 open, 1 closed (#7, already shipped in iteration 7) — nothing new to slot into the Feature backlog. Found the repo in a messy state from several prior automated runs that had re-derived iteration 20's exact scope on fresh `claude/peaceful-cannon-*` branches without ever merging: 6 duplicate open PRs (#35–#40, all "2 Samuel 13-14") plus one closed-unmerged PR (#42) with identical content — closed all 6 duplicates as superseded once confirmed iteration 20's commit (`70ecc17`) was in fact already on `main` (a squash/direct-push landed it outside this loop's own merge step). Then continued the roadmap: curated 2 Sam 16 (Ziba's slander of Mephibosheth, Shimei's cursing/stone-throwing restrained by David, Ahithophel's roof-tent counsel fulfilling Nathan's 12:11-12 oracle, M) + 2 Sam 17 (Ahithophel's rejected night-strike plan vs. Hushai's counter-counsel, the well at Bahurim, Ahithophel's suicide, the Jordan crossing, provisioning at Mahanaim, M) — continues the "2 Samuel full-book gaps" item; new place `bahurim`; new people `shimei`, `barzillai`; reused `donkey`/`cistern` objects and `jerusalem`/`olives`/`mahanaim`/`jordan`/`lo-debar`/`ziba`/`mephibosheth`/`ahithophel`/`hushai`; fixtures 10-16/10-17; sweep entries added — 522 assertions, PASS, no regressions. | 1× sonnet ENG (curation) + orchestrator (PR cleanup) | ✅ shipped |
| 22 | 2026-07-31 | Re-scanned all GitHub issues (open+closed): still only issue #7 total, ever filed, closed, already shipped in iteration 7 — nothing new to slot into the Feature backlog. Found and closed one more stale unmerged duplicate PR (#32, "2 Samuel 9 and 10", opened 2026-07-17) — confirmed 2 Sam 9-10 already on `main` since iteration 18, same root cause as the 6 duplicates iteration 21 cleaned up. Curated 2 Sam 19 (David's return, Shimei spared, Mephibosheth/Ziba split, Barzillai declines reward, M) + 2 Sam 20 (Sheba's revolt, Amasa murdered by Joab at Gibeon, siege of Abel Beth-maacah ended by its wise woman, M) + 2 Sam 21 (three-year famine, Gibeonites' seven executed, Rizpah's vigil, Saul/Jonathan reburied at Zela, four Philistine-giant duels, M) — closes the "2 Samuel full-book gaps" item down to ch. 22-23 only; new places `gob`, `zela`, `abel-beth-maacah` (all with `meaning` from birth); new people `amasa`, `sheba-bichri`, `wise-woman-abel`, `rizpah`, `gibeonites`, `philistine-giants` (grouped, matching the `cherethites-pelethites` precedent), `elhanan`; no new objects (reused `sackcloth`/`spear`/`sword`); fixtures 10-19/10-20/10-21; sweep entries added — 540 assertions, PASS, no regressions. | 1× sonnet ENG (curation) + orchestrator (issue scan, PR cleanup, integration) | ✅ shipped |
| 23 | 2026-08-03 | Curated 2 Sam 23 (David's last words — the "Rock of Israel" oracle, vv. 1-7 — and the roster of the mighty men: Josheb-basshebeth/Adino, Eleazar's hand frozen to his sword, Shammah's lentil-field stand, the Three breaking the Philistine camp at Bethlehem for well water and David pouring it out to the LORD, Benaiah's lion-in-the-pit and Egyptian-spearman feats, the Thirty's roster closing on Uriah the Hittite, M) — finishes the "2 Samuel full-book gaps" item down to a deliberate skip on ch. 22 only (David's song, a near-duplicate of Psalm 18 — pure poetry, no narrative geography to map; per this Ledger's own note it does not get a full curated entry, and the automatic pipeline already renders it fine). New people `josheb-basshebeth`, `eleazar-dodo`, `shammah-agee` (the Three, kept minimal per the iteration-19 compressed-M-depth precedent); no new places (fully reused `bethlehem`/`adullam`/`tekoa`/`anathoth`/`gibeah`/`zobah`/`jerusalem`) or new objects (reused `spear`/`sword`/`well`); reused existing people `david`/`joab`/`abishai`/`benaiah`/`uriah`/`elhanan` (all already in the dataset from earlier iterations, per the task's dedup precedent); fixture `tests/fixtures/10-23.json` (39/39 verses); sweep entry `"2 Samuel 23\|10:23"` added — 546 assertions, PASS, no regressions ([PR #45](https://github.com/elinxie/Bible-verse-visualizer/pull/45), draft, awaiting human review/merge per this repo's convention — see prior PRs #43/#44). | 1× sonnet ENG (curation) | ✅ shipped, PR open |
| 24 | 2026-08-04 | Re-scanned all GitHub issues (open+closed): still only issue #7 total, ever filed, closed, already shipped in iteration 7 — nothing new to slot into the Feature backlog. Found PR #45 (iteration 23's 2 Sam 23 work) open and unmerged from a prior session — verified locally (`node tests/smoke.js`, 546/546 assertions PASS) and merged it to `main` directly rather than re-deriving the same scope, per the NEXT SESSION note's own warning about the duplicate-PR failure mode; confirmed the post-merge Pages deploy started successfully. Continued the "Then" roadmap: curated Exodus 3 (M) — Moses at the burning bush on Horeb, the unconsumed bush, the divine name revealed ("I AM WHO I AM" / YHWH), the call to deliver Israel, Moses' objections, the plundering-of-Egypt promise; new person `jethro` (Moses' father-in-law, priest of Midian); new object `burning-bush`; no new places (reused `midian`/`sinai`/`egypt`); fixture `tests/fixtures/2-3.json` (22/22 verses); sweep entry `"Exodus 3\|2:3"` added (first Exodus entry in `CURATED_SWEEP` — Exodus 14 was curated in an earlier iteration but never added to the sweep array) — 547 assertions, PASS, no regressions. | 1× sonnet ENG (curation) + orchestrator (PR merge, integration) | ✅ shipped |
| 25 | 2026-08-05 | Re-scanned all GitHub issues (open+closed) and open PRs: still only issue #7 total, ever filed, closed, already shipped in iteration 7 — nothing new to slot into the Feature backlog; no open PRs found (prior duplicate-PR pileup fully cleaned as of iteration 24). Curated Exodus 12 (M) — the Passover institution, blood on the doorposts, the meal eaten in haste, the tenth plague at midnight, Pharaoh's expulsion of Israel, the departure from Rameses 430 years after Jacob's arrival — continues the "Then" roadmap; new person `pharaoh-exodus` (distinct from existing `pharaoh-joseph`); new object `hyssop`; reused existing places `egypt`/`goshen`/`rameses` (already had `ancient`+`scene` from the iteration-24 Exodus batch) and existing people `moses`/`aaron`, plus existing objects `passover-lamb`/`unleavened`; fixture `tests/fixtures/2-12.json` (51/51 verses); sweep entry `"Exodus 12\|2:12"` added — smoke suite green, no regressions. | 1× sonnet ENG (curation) | ✅ shipped |
| 26 | 2026-08-06 | Re-scanned all GitHub issues (open+closed): still only issue #7 total, ever filed, closed, already shipped in iteration 7 — nothing new to slot into the Feature backlog. Found PR #47 (Exodus 12, from a prior session) open and unmerged — verified locally (`node tests/smoke.js`, PASS, no regressions) and merged it to `main` directly rather than re-deriving the same scope, continuing the established stale-PR cleanup pattern. Curated Exodus 19 (Israel's arrival at Sinai, the "kingdom of priests" covenant proposal, the mountain quarantine/boundary, the third-day theophany in fire/smoke/thunder, M) + Exodus 20 (the Ten Commandments spoken directly to the assembled people, vv. 1-17; their terrified request that Moses mediate going forward, vv. 18-21; the closing altar law, vv. 22-26, M) + Exodus 32 (the golden calf while Moses is on the mountain, Aaron's role and excuse, Moses breaking the tablets, grinding the calf into the drinking water, the Levites rallying to "Who is on the LORD's side?", the plague, M) — continues the "Then" roadmap, completing the Exodus key-chapter batch through ch. 32. New person `levites` (the tribe collectively, matching the `cherethites-pelethites`/`philistine-giants`/`gibeonites` group-entry precedent); new objects `ten-commandments` (the spoken Decalogue), `tablets-stone` (the physical stone tablets Moses later breaks), `golden-calf`; no new places needed — all three chapters fully reused existing `sinai` (whose `al` already covers "Horeb") and `egypt`, plus existing people `moses`/`aaron`. Fixtures `tests/fixtures/2-19.json` (25/25 verses), `2-20.json` (26/26 verses), `2-32.json` (35/35 verses); sweep entries `"Exodus 19\|2:19"`, `"Exodus 20\|2:20"`, `"Exodus 32\|2:32"` added to `CURATED_SWEEP`. `node --check` clean on all five touched files; full offline Playwright suite (`PLAYWRIGHT_BROWSERS_PATH=/opt/pw-browsers node tests/smoke.js`) — 576 assertions, PASS, no regressions, independently re-verified by the orchestrator before merge ([PR #48](https://github.com/elinxie/Bible-verse-visualizer/pull/48), merged). | 1× sonnet ENG (curation) + orchestrator (issue scan, PR #47 merge, independent test re-verification, PR #48 merge) | ✅ shipped |
| 27 | 2026-08-07 | Re-scanned all GitHub issues (open+closed): still only issue #7 total, ever filed, closed, already shipped in iteration 7 — nothing new to slot into the Feature backlog; no open PRs found (clean state at session start). Curated Joshua 2 (Rahab shelters the two spies at Jericho, her confession of faith, the scarlet-cord sign, M) + Joshua 6 (the seven-day march around Jericho, the trumpets and the ark, the shout, the wall's fall, herem, Rahab's household spared, the curse on rebuilding) — starts the "Then" roadmap's post-Exodus item; new person `rahab`; new object `scarlet-cord`; no new places (fully reused `jericho`/`jordan`/`gilgal`/`moab-plains`, the latter's existing `ancient` note already covering Shittim); reused existing `joshua` person and `ark-covenant`/`trumpet` objects; fixtures 6-2/6-6 (24/27 verses); sweep entries `"Joshua 2\|6:2"`, `"Joshua 6\|6:6"` added — 588 assertions, PASS, no regressions, independently re-verified by the orchestrator (`node --check` + full offline Playwright suite) before commit. | 1× sonnet ENG (curation) | ✅ shipped |
| 28 | 2026-08-08 | Re-scanned all GitHub issues (open+closed): still only issue #7 total, ever filed, closed, already shipped in iteration 7 — nothing new to slot into the Feature backlog. Found PR #50 (iteration 27's Joshua 2/6 work) open as a **draft** — verified locally (`node tests/smoke.js`, 588/588 assertions PASS, no regressions), marked ready for review, and squash-merged it to `main` directly rather than re-deriving the same scope, continuing the established stale/unmerged-PR cleanup pattern (iterations 21, 22, 24, 26). Continued the "Then" roadmap: curated Judges 4 (Deborah/Barak, Sisera's chariots routed at the Kishon, Jael's tent peg, M) + Judges 7 (Gideon's water test at the spring of Harod, the barley-loaf dream, trumpets/jars/torches night raid, Oreb & Zeeb, M) + Judges 16 (Samson and Delilah — three false tests then the true secret, Gaza's gates, blinding and imprisonment, hair regrowth, pulling down Dagon's temple, M) — closes out the Judges key-chapter batch. New places `kishon`, `harosheth-hagoyim`, `sorek` (all with `meaning` from birth); new people `barak`, `sisera`, `jael`, `delilah`, plus group entries `midianites`/`philistine-lords` (matching the `philistine-giants`/`gibeonites` precedent); reused existing `deborah`/`gideon`/`samson`/`tabor`/`hazor`/`jezreel-spring`/`moreh`/`midian`/`gaza`/`hebron` (checked for id collisions first, per the `gad`/`gad-son-jacob` and `tamar-david`/`tamar-judah` precedent — none found); new objects `tent-peg`, `torch-jar`, `delilah-tests`, `razor`, `temple-pillars`; fixtures 7-4/7-7/7-16 (24/24, 25/25, 31/31 verses); sweep entries `"Judges 4\|7:4"`, `"Judges 7\|7:7"`, `"Judges 16\|7:16"` added to `CURATED_SWEEP` — 606 assertions, PASS, independently re-verified by the orchestrator (`node --check` on all 5 touched files + full offline Playwright suite) before commit, no regressions. | 1× sonnet ENG (curation) + orchestrator (issue scan, PR #50 merge, independent test re-verification) | ✅ shipped |
| 29 | 2026-08-09 | Re-scanned all GitHub issues (open+closed): still only issue #7 total, ever filed, closed, already shipped in iteration 7 — nothing new to slot into the Feature backlog. Found PR #51 (iteration 28's Judges 4/7/16 work, already containing its own Ledger row) open as a **draft** — verified locally (`node --check` on all touched JS + `PLAYWRIGHT_BROWSERS_PATH=/opt/pw-browsers node tests/smoke.js`, 606/606 assertions PASS, no regressions), marked ready for review, and squash-merged it to `main` directly rather than re-deriving the same scope, continuing the established stale/draft-PR cleanup pattern (iterations 21, 22, 24, 26, 28). No new curation this session (pure issue-rescan + stale-PR cleanup iteration, matching the iteration-8/9/11 pattern) — confirmed the post-merge "Deploy to GitHub Pages" run started (run #53, in_progress at merge time; not babysat further per the no-polling guardrail). | orchestrator only (issue scan, PR #51 merge, independent test re-verification) | ✅ shipped |
| 30 | 2026-08-10 | Re-scanned all GitHub issues (open+closed) via `list_issues`: still only issue #7 total, ever filed, closed, already shipped in iteration 7 — nothing new to slot into the Feature backlog. Found PR #52 (iteration 29's doc-only Ledger update) open as a **draft** — verified the diff matched actual repo state (PR #51/Judges 4,7,16 was already merged to `main` as `065f1d3`), marked it ready for review, and squash-merged it directly rather than re-deriving the same scope, continuing the established stale/draft-PR cleanup pattern (iterations 21, 22, 24, 26, 28, 29). Continued the "Then" roadmap: curated Ruth 1–4 (M, compact depth per the iteration-19/28 cost-control precedent) — famine/move to Moab and the deaths of Elimelech/Mahlon/Chilion (ch. 1), Ruth's declaration of loyalty to Naomi, Orpah turning back, the return to Bethlehem at the start of barley harvest; Ruth gleaning in Boaz's field "as it happened," his kindness (ch. 2); Naomi's threshing-floor plan, Ruth's request under Boaz's garment as next-of-kin (ch. 3); the city-gate proceedings, the nearer redeemer's refusal and the sandal-transfer custom, Boaz's redemption and marriage to Ruth, Obed's birth, and the closing genealogy from Perez to David (ch. 4) — finishes the whole book in one batch. New people `naomi`, `boaz`, `elimelech`, `mahlon`, `chilion`, `orpah`, `obed`, `nearer-redeemer` (checked for id collisions against the existing dataset first, per the `gad`/`tamar` precedent — none found; existing `ruth` stub reused as-is, existing `david` referenced via crossRefs for the closing genealogy rather than recreated); new objects `sheaf`, `ephah-barley`, `sandal`, `garment-corner` (reused existing `threshing`); no new places (reused `bethlehem`/`moab-plains`, the latter topped up with a `meaning` field). Fixtures `tests/fixtures/8-1.json`–`8-4.json` (22/23/18/22 verses); sweep entries `"Ruth 1\|8:1"`…`"Ruth 4\|8:4"` added to `CURATED_SWEEP` — full offline Playwright suite green, no regressions, independently re-verified by the orchestrator (`node --check` on all 5 touched files + full suite) before commit. Ruth is now the second full book (after Genesis and 2 Samuel) complete in `BVV.CURATED`. | 1× sonnet ENG (curation) + orchestrator (issue scan, PR #52 merge, independent test re-verification) | ✅ shipped |
| 31 | 2026-08-11 | Re-scanned all GitHub issues (open+closed) via `list_issues`: still only issue #7 total, ever filed, closed, already shipped in iteration 7 — nothing new to slot into the Feature backlog. No open PRs found (clean state at session start; no stale-PR cleanup needed this time). Curated 1 Kings 3 (Solomon's marriage alliance with Pharaoh's daughter, the sacrifice/dream at Gibeon asking for wisdom over riches, God's grant of wisdom/riches/honor, the judgment of the two mothers over one living child, M) + 1 Kings 8 (the ark brought up from the City of David, the glory-cloud filling the temple so the priests could not minister, Solomon's dedication speech and long prayer — covenant faithfulness, forgiveness, prayer toward the temple, the foreigner's prayer, exile and return — fire/glory falling, the closing 14-day feast, M) — starts the "Then" roadmap's post-Ruth item (1 Kings 3, 8, 17–19); new people `pharaohs-daughter` (kept distinct from `pharaoh-exodus`/`pharaoh-joseph` per the `gad`/`tamar` disambiguation precedent), `two-mothers` (group entry matching the `philistine-giants`/`gibeonites` precedent); new object `temple-glory-cloud`; no new places (fully reused `gibeon`/`jerusalem`/`egypt`, all already complete from earlier iterations); reused existing `solomon`/`zadok`/`ark-covenant`/`temple` entries. Fixtures `tests/fixtures/11-3.json` (28/28 verses), `11-8.json` (66/66 verses); sweep entries `"1 Kings 3\|11:3"`, `"1 Kings 8\|11:8"` added to `CURATED_SWEEP` — 642 assertions, PASS, no regressions, independently re-verified by the orchestrator (`node --check` on all touched files + full offline Playwright suite, after a one-time `npm i playwright --no-save` in this fresh container) before commit. | 1× sonnet ENG (curation) + orchestrator (issue scan, independent test re-verification) | ✅ shipped |
| 32 | 2026-08-12 | Re-scanned all GitHub issues (open+closed) via `list_issues`: still only issue #7 total, ever filed, closed, already shipped in iteration 7 — nothing new to slot into the Feature backlog (confirms the "NEXT SESSION" note's scan). Found PR #54 (iteration 31's 1 Kings 3/8 work, already containing its own Ledger row) open as a **draft**, branched correctly off the post-Ruth `main` — independently re-verified locally (`node --check` on all 4 touched JS/test files + a fresh `npm i playwright --no-save` + full offline Playwright suite, `PLAYWRIGHT_BROWSERS_PATH=/opt/pw-browsers node tests/smoke.js`, 642/642 assertions PASS, no regressions), marked ready for review, and squash-merged it to `main` directly rather than re-deriving the same scope, continuing the established stale/draft-PR cleanup pattern (iterations 21, 22, 24, 26, 28, 29, 30). No new curation this session (pure issue-rescan + stale-PR cleanup iteration, matching the iteration-8/9/11/29 pattern). | orchestrator only (issue scan, PR #54 merge, independent test re-verification) | ✅ shipped |
| 32 | 2026-08-12 | Re-scanned all GitHub issues (open+closed) via `list_issues`: still only issue #7 total, ever filed, closed, already shipped in iteration 7 — nothing new to slot into the Feature backlog. Found PR #54 (iteration 31's 1 Kings 3/8 work) open as a **draft** — verified locally (642/642 assertions PASS, no regressions), marked ready for review, and squash-merged it to `main` directly rather than re-deriving the same scope, continuing the established stale/draft-PR cleanup pattern. Doc-only iteration: Ledger row + next-session note (PR #55). | orchestrator only (issue scan, PR #54 merge) | ✅ shipped |
| 33 | 2026-08-13 | Re-scanned all GitHub issues (open+closed) via `list_issues`: still only issue #7 total, ever filed, closed, already shipped in iteration 7 — nothing new to slot into the Feature backlog. Found two stale **draft** PRs open at session start: #55 (iteration 32's doc-only Ledger update) and #56 (a small wording fix replacing `fable` with `opus`/Opus 5 as the loop's architecture-tier model in the Budget model section, per an explicit prior user request) — both based cleanly on the current `main`, non-conflicting (different sections of `PLAYBOOK.md`), verified by reading their diffs, marked ready for review, and squash-merged directly rather than re-deriving either scope, continuing the established stale/draft-PR cleanup pattern (iterations 21, 22, 24, 26, 28, 29, 30, 32). Continued the "Then" roadmap: curated 1 Kings 17 (Elijah announces the drought, fed by ravens at the brook Cherith, M) + 1 Kings 18 (the widow of Zarephath's jar of flour/jug of oil and her son raised — carried over from ch. 17's back half — the Mount Carmel contest with the 450 prophets of Baal, fire from heaven, the prophets killed at the Kishon, the rain returns, M) + 1 Kings 19 (Elijah's flight from Jezebel to Beersheba then Horeb, the still small voice, the three commissions — Hazael, Jehu, Elisha, M) — finishes the "1 Kgs 17–19" roadmap item. New place `cherith` (the disputed Gilead wadi where the ravens fed Elijah); new people `widow-zarephath`, `obadiah-steward` (Ahab's steward who hid 100 prophets — disambiguated from the later minor prophet, per the `gad`/`tamar` precedent), `hazael`, `jehu`; new objects `ravens-cherith`, `widows-jar-oil`, `carmel-fire`, `still-small-voice`; reused existing places `zarephath`, `carmel-mount`, `kishon`, `jezreel-city`, `beersheba`, `sinai` (already aliased to Horeb) and existing people `elijah`/`ahab`/`jezebel`/`elisha` — all checked for id collisions first, none found. Fixtures `tests/fixtures/11-17.json` (24/24 verses), `11-18.json` (46/46 verses), `11-19.json` (21/21 verses); sweep entries `"1 Kings 17\|11:17"`, `"1 Kings 18\|11:18"`, `"1 Kings 19\|11:19"` added to `CURATED_SWEEP`. `node --check` clean on all five touched files, zero duplicate ids across places/people/objects (independently re-verified by the orchestrator via a standalone Node script), full offline Playwright suite (`npm i playwright --no-save` + `PLAYWRIGHT_BROWSERS_PATH=/opt/pw-browsers node tests/smoke.js`) — PASS, no regressions. | 1× sonnet ENG (curation) + orchestrator (issue scan, PR #55/#56 merge, independent id-collision + test re-verification) | ✅ shipped |
| 34 | 2026-08-14 | Re-scanned all GitHub issues (open+closed): still only issue #7 total, ever filed, closed, already shipped in iteration 7 — nothing new to slot into the Feature backlog. No open PRs found (clean state at session start; `origin/main` synced to `1b0abdd`, iteration 33's 1 Kings 17-19 work already merged). Curated 2 Kings 5 (Naaman's healing and conversion, Gehazi's greed and leprosy, M) + 2 Kings 18 (Hezekiah's reforms incl. breaking Nehushtan, Sennacherib's invasion, the Rabshakeh's taunt at the wall, M) + 2 Kings 19 (Hezekiah's prayer, Isaiah's oracles, the angel striking down 185,000, Sennacherib's assassination, M) + 2 Kings 25 (the fall of Jerusalem, Zedekiah's capture and blinding, the temple/palace burned, the exile to Babylon, Gedaliah's assassination, Jehoiachin's release, M) — completes the "2 Kgs 5, 18–19, 25" roadmap item. New people `naaman`, `gehazi`, `sennacherib`, `rabshakeh`, `nebuchadnezzar`, `zedekiah`, `gedaliah`, `nebuzaradan`, `jehoiachin`, `evil-merodach`; new place `riblah` (Nebuchadnezzar's field HQ where Zedekiah's sons were killed before him); new objects `naaman-gift`, `nehushtan`, `temple-treasures-babylon`; reused existing places `jerusalem`/`samaria`/`lachish`/`damascus`/`babylon`/`nineveh`/`jordan`/`jericho`/`mizpah` and existing people `elisha`/`hezekiah`/`isaiah` (all checked for id collisions first — none found, zero duplicate ids across people/places/objects independently re-verified by the orchestrator). Fixtures `tests/fixtures/12-5.json` (27/27 verses), `12-18.json` (37/37), `12-19.json` (37/37), `12-25.json` (30/30); sweep entries `"2 Kings 5\|12:5"`, `"2 Kings 18\|12:18"`, `"2 Kings 19\|12:19"`, `"2 Kings 25\|12:25"` added to `CURATED_SWEEP`. `node --check` clean on all 5 touched JS files; full offline Playwright suite (`npm i playwright --no-save` + `PLAYWRIGHT_BROWSERS_PATH=/opt/pw-browsers node tests/smoke.js`) — PASS, no regressions, independently re-verified by the orchestrator before commit. | 1× sonnet ENG (curation) + orchestrator (issue scan, independent id-collision + test re-verification) | ✅ shipped |
| 35 | 2026-08-14 | Continuation of the same day's session, user asked to keep going past iteration 34's stop point. Marked PR #58 (iteration 34's 2 Kings 5/18/19/25 work) ready for review and squash-merged it to `main`; restarted the designated branch from the new `main` tip (force-with-lease push, safe since the old branch's tree content was already identical to what landed via the squash — verified with an empty `git diff`). Curated Ezra 3 (the returned exiles rebuild the altar and lay the temple's foundation, the mixed weeping/rejoicing of the two generations, M) + Nehemiah 2 (Nehemiah's request to Artaxerxes, the night ride around Jerusalem's ruined walls, opposition from Sanballat/Tobiah/Geshem, M) + Nehemiah 8 (Ezra's public reading of the Law at the Water Gate, the people's weeping turned to commanded joy, the rediscovered Feast of Booths, M) + Esther 4 (Mordecai's sackcloth mourning at Haman's decree, "for such a time as this," Esther's "if I perish, I perish," M) — completes the "Ezra 3, Neh 2 & 8, Esther 4" roadmap item. New people `zerubbabel`, `jeshua-priest` (disambiguated from the existing `joshua` id per the `gad`/`tamar` precedent), `artaxerxes`, `sanballat`, `tobiah`, `mordecai`, `haman`, `ahasuerus`, `hathach`; no new places (reused `jerusalem`/`babylon`/`susa`/`joppa`, all already complete) or new objects (reused `altar`/`trumpet`/`scroll`/`sackcloth`) — all checked for id collisions first, none found, zero duplicate ids across people/places/objects independently re-verified by the orchestrator. Fixtures `tests/fixtures/15-3.json` (13/13 verses), `16-2.json` (20/20), `16-8.json` (18/18), `17-4.json` (17/17), all confirmed sequential with no gaps; sweep entries `"Ezra 3\|15:3"`, `"Nehemiah 2\|16:2"`, `"Nehemiah 8\|16:8"`, `"Esther 4\|17:4"` added to `CURATED_SWEEP`. `node --check` clean on all touched files; full offline Playwright suite (`PLAYWRIGHT_BROWSERS_PATH=/opt/pw-browsers node tests/smoke.js`) — PASS, no regressions, independently re-verified by the orchestrator before commit. | 1× sonnet ENG (curation) + orchestrator (PR #58 merge, branch restart, independent id-collision + test re-verification) | ✅ shipped |
| 36 | 2026-08-15 | Re-scanned all GitHub issues (open+closed) via `list_issues`: still only issue #7 total, ever filed, closed, already shipped in iteration 7 — nothing new to slot into the Feature backlog. No open PRs found (clean state at session start; `origin/main` synced to `9d900de`, iteration 35's Ezra 3/Neh 2 & 8/Esther 4 work already merged). Continued the "Then" roadmap: curated Job 1 (the heavenly-council wager over Job's possessions, the four rapid-fire messengers, "the LORD gave, and the LORD has taken away", M) + Job 2 (the second wager over Job's body, the sores, his wife's "curse God and die", the three friends' seven days of silent mourning, M) + Job 38 (the LORD's answer out of the whirlwind — cosmology/creation interrogation, the wild-animal roll call begins, M) — closes the "Job 1–2, 38" roadmap item. New people `ha-satan` (deliberately disambiguated from a bare `satan` id per the `gad`/`tamar` precedent, since this is the heavenly-council office/title, not the fully personalized NT devil-figure), `eliphaz`, `bildad`, `zophar`, `job-wife`, `job-messengers` (group entry, matching the `philistine-giants`/`gibeonites` precedent); reused existing `job` person id as-is. New place `uz` (disputed Edom-borderland site, noted honestly per the `cherith`/`eden` disputed-site convention, with `meaning` from birth per issue #7's convention); `meaning` top-up on `edom` (its dashed-journey second marker). New objects `job-sores`, `whirlwind` (reused existing `robe`/`sackcloth` for the mourning gestures). All 9 new ids checked for collisions first — zero duplicates, independently re-verified by the orchestrator via a standalone Node script across all three data files. Fixtures `tests/fixtures/18-1.json` (22/22 verses), `18-2.json` (13/13), `18-38.json` (41/41); sweep entries `"Job 1|18:1"`, `"Job 2|18:2"`, `"Job 38|18:38"` added to `CURATED_SWEEP`. `node --check` clean on all 5 touched files; full offline Playwright suite (`PLAYWRIGHT_BROWSERS_PATH=/opt/pw-browsers node tests/smoke.js`) — 726 assertions, PASS, no regressions, independently re-verified by the orchestrator before commit. | 1× sonnet ENG (curation) + orchestrator (issue scan, independent id-collision + test re-verification) | ✅ shipped |
| 37 | 2026-08-22 | Re-scanned all GitHub issues (open+closed) via `list_issues`: still only issue #7 total, ever filed, closed, already shipped in iteration 7 — nothing new to slot into the Feature backlog. Found PR #60 (iteration 36's Job 1/2/38 work) open as a **draft** — independently re-verified locally (`node --check` on all touched files + a fresh `npm i playwright --no-save` + full offline Playwright suite, 726/726 assertions PASS, no regressions), marked ready for review, and squash-merged it to `main` directly rather than re-deriving the same scope, continuing the established stale/draft-PR cleanup pattern (iterations 21, 22, 24, 26, 28, 29, 30, 32, 33, 34). Restarted the designated branch from the new `main` tip. Continued the "Then" roadmap: curated Psalm 22 (the cry of dereliction and its turn to praise, mapped thematically to Jerusalem's temple-worship setting with a dashed leg from Bethlehem, M) + Psalm 23 ("The LORD is my shepherd," mapped to Bethlehem's pasture country with a dashed leg to Jerusalem, M) + Psalm 51 (David's repentance psalm after Nathan's rebuke, mapped to Jerusalem with a dashed leg to Rabbah/2 Sam 11's siege, M) + Psalm 137 (the Babylonian-exile lament, mapped to Babylon/Jerusalem with a dashed leg to Edom, M) — closes the "Pss 22, 23, 51, 137" roadmap item. Since these are poetry, not narrative, each entry maps thematically rather than to a plotted route and says so explicitly in-text. New objects `casting-lots` (Ps 22:18), `shepherds-rod-staff` (Ps 23:4); new person `babylonian-exiles` (group entry, matching the `job-messengers`/`gibeonites` precedent); reused existing people `david`/`nathan`/`bathsheba` and objects `hyssop`/`anoint-oil`/`lyre`; no new places (reused `jerusalem`/`bethlehem`/`babylon`/`edom`/`rabbah`, all already complete except `babylon`, topped up with a missing `meaning` field — "Akkadian 'Bab-ilu' — gate of god(s)" — per issue #7's convention). All 3 new ids checked for collisions first — zero duplicates, independently re-verified by the orchestrator via a standalone Node script across all three data files (246 people / 147 places / 109 objects). Fixtures `tests/fixtures/19-22.json` (31/31 verses), `19-23.json` (6/6), `19-51.json` (19/19), `19-137.json` (9/9); sweep entries `"Psalms 22\|19:22"`, `"Psalms 23\|19:23"`, `"Psalms 51\|19:51"`, `"Psalms 137\|19:137"` added to `CURATED_SWEEP`. `node --check` clean on all 5 touched files; full offline Playwright suite (`PLAYWRIGHT_BROWSERS_PATH=/opt/pw-browsers node tests/smoke.js`) — 750 assertions, PASS, no regressions, independently re-verified by the orchestrator before push. | 1× sonnet ENG (curation) + orchestrator (issue scan, PR #60 merge, branch restart, independent id-collision + test re-verification) | ✅ shipped |
| 38 | 2026-08-23 | Task: scan every GitHub issue ever filed and slot each into the Feature backlog in oldest-to-newest order, wherever it fits, so work can start on it. `list_issues` (state=OPEN, then state=CLOSED) confirms the repo has exactly one issue ever filed, total: #7, closed 2026-07-05 — already fully reflected in the Feature backlog table (shipped iteration 7, with later data top-ups tracked through iteration 36). No new issue to slot. Found PR #61 (iteration 37's Psalms 22/23/51/137 work, already carrying its own Ledger row) open as a **draft** — independently re-verified locally (`node --check` on all 5 touched JS/test files, a standalone Node id-collision scan across `places.js`/`people.js`/`objects.js` confirming 147/246/109 entries with zero duplicate ids, and a fresh `npm i playwright --no-save` + full offline Playwright suite: 750/750 assertions PASS, no regressions), marked ready for review, and squash-merged it to `main` directly rather than re-deriving the same scope, continuing the established stale/draft-PR cleanup pattern (iterations 21, 22, 24, 26, 28, 29, 30, 32, 33, 34, 37). Restarted the designated branch from the new `main` tip. No new curation this session (pure issue-rescan + stale-PR cleanup iteration, matching the iteration-8/9/11/29/32 pattern). | orchestrator only (issue scan, PR #61 merge, independent id-collision + test re-verification) | ✅ shipped |
| 39 | 2026-08-24 | Task: scan every GitHub issue ever filed and slot each into the Feature backlog in oldest-to-newest order, wherever it fits, so work can start on it. `list_issues` (state=OPEN, then state=ALL) confirms the repo has exactly one issue ever filed, total: #7, closed 2026-07-05 — already fully reflected in the Feature backlog table (shipped iteration 7, with later data top-ups tracked through iteration 37). No new issue to slot. Found PR #62 (iteration 38's doc-only Ledger update) open as a **draft** — verified its diff matched actual repo state (PR #61/Pss 22-137 was already merged to `main` as `de76347`), marked it ready for review, and squash-merged it directly, continuing the established stale/draft-PR cleanup pattern (iterations 21, 22, 24, 26, 28, 29, 30, 32, 33, 34, 37, 38). Restarted the designated branch from the new `main` tip and pushed it (the branch had no remote counterpart yet this session). Continued the "Then" roadmap: dispatched a sonnet ENG agent to curate Isaiah 6 (the temple vision — seraphim, "Holy, holy, holy," the live coal, "Here am I, send me," the hardening commission, M) + Isaiah 40 ("Comfort my people," the wilderness herald, "all flesh is grass," the shepherd, "renew their strength," M) + Isaiah 53 (the Suffering Servant song — "wounded for our transgressions," "led as a lamb to the slaughter," a rich man's grave, M) — closes the "Isa 6, 40, 53" roadmap item. Since these are prophetic poetry, not narrative, each entry maps thematically (Isa 6 → Jerusalem's temple with a dashed leg to Nineveh/Assyria's shadow; Isa 40 → Jerusalem with a dashed leg from Babylon/the exile audience; Isa 53 → Jerusalem with a dashed leg from Bethlehem, mirroring the Ps 22 life-arc precedent) and says so explicitly in-text, per the Pss 22/23/51/137 precedent. New objects `seraphim`, `altar-coal` (Isa 6), `grass-flower` (Isa 40), `lamb-to-slaughter` (Isa 53); no new places or people — fully reused existing `jerusalem`/`bethlehem`/`babylon`/`nineveh` and `isaiah`, all checked for id collisions first, none found; `nineveh` topped up with a missing `meaning` field per issue #7's convention. Fixtures `tests/fixtures/23-6.json` (13/13 verses), `23-40.json` (31/31), `23-53.json` (12/12); sweep entries `"Isaiah 6\|23:6"`, `"Isaiah 40\|23:40"`, `"Isaiah 53\|23:53"` added to `CURATED_SWEEP`. `node --check` clean on all 4 touched JS files; zero duplicate ids across places/people/objects (147/246/113, independently re-verified by the orchestrator via a standalone Node script — matches the agent's own count); full offline Playwright suite (`npm i playwright --no-save` + `PLAYWRIGHT_BROWSERS_PATH=/opt/pw-browsers node tests/smoke.js`) — 768 assertions, PASS, no regressions, independently re-verified by the orchestrator before commit. | 1× sonnet ENG (curation) + orchestrator (issue scan, PR #62 merge, branch push, independent id-collision + test re-verification) | ✅ shipped |
| 40 | 2026-08-25 | Task: scan every GitHub issue ever filed and slot each into the Feature backlog in oldest-to-newest order, wherever it fits, so work can start on it. `list_issues` (state=OPEN, then state=ALL) confirms the repo has exactly one issue ever filed, total: #7, closed 2026-07-05 — already fully reflected in the Feature backlog table (shipped iteration 7, with later data top-ups tracked through iteration 39). No new issue to slot. Found PR #63 (iteration 39's Isaiah 6/40/53 work, already carrying its own Ledger row) open as a **draft** — independently re-verified in an isolated worktree (`node --check` on all 4 touched JS files; a standalone Node id-collision scan across `places.js`/`people.js`/`objects.js` confirming 147/246/113 entries with zero duplicate ids, matching the PR's own count; a fresh `npm i playwright --no-save` + full offline Playwright suite: 768/768 assertions PASS, no regressions), marked ready for review, and squash-merged it to `main` directly rather than re-deriving the same scope, continuing the established stale/draft-PR cleanup pattern (iterations 21, 22, 24, 26, 28, 29, 30, 32, 33, 34, 37, 38, 39). Restarted the designated branch from the new `main` tip. No new curation this session (pure issue-rescan + stale-PR cleanup iteration, matching the iteration-8/9/11/29/32/38 pattern). | orchestrator only (issue scan, PR #63 merge, independent id-collision + test re-verification) | ✅ shipped |
| 40 | 2026-08-25 | Task: scan every GitHub issue ever filed and slot each into the Feature backlog in oldest-to-newest order. `list_issues` (state=OPEN, then ALL) confirms the repo has exactly one issue ever filed, total: #7, closed 2026-07-05 — already fully reflected in the Feature backlog table. No new issue to slot. Found PR #63 (iteration 39's Isaiah 6/40/53 work, already carrying its own Ledger row) open as a **draft** — independently re-verified (`node --check` on all 4 touched JS files; standalone id-collision scan across places/people/objects, 147/246/113 entries, zero dupes; fresh `npm i playwright --no-save` + full offline Playwright suite, 768/768 assertions PASS, no regressions), marked ready for review, and squash-merged it to `main` directly, continuing the established stale/draft-PR cleanup pattern. Doc-only iteration: Ledger row + next-session note (PR #64). | orchestrator only (issue scan, PR #63 merge, independent id-collision + test re-verification) | ✅ shipped |
| 41 | 2026-08-26 | Scheduled task: re-scanned every GitHub issue ever filed (`list_issues` OPEN then ASC-CREATED, no state filter): still exactly one, #7, closed 2026-07-05, already fully reflected in the Feature backlog — nothing new to slot. Found PR #64 (iteration 40's doc-only Ledger update) open as a **draft** — verified its diff was a clean, safe `PLAYBOOK.md`-only change matching actual repo state (Isaiah 6/40/53 already merged as `8153471`), marked it ready for review, and squash-merged it to `main` directly. Restarted the designated branch from the fresh `main` tip and pushed it (no remote counterpart existed yet this session). Continued the "Then" roadmap: dispatched a sonnet ENG agent to curate **Jeremiah 1** (the call at Anathoth — the child's objection, the LORD touching his mouth, the almond-branch and boiling-pot visions, the pluck-up/build-plant commission, M) + **Jeremiah 29** (the letter to Babylon's exiles — build houses, seek the city's welfare, false prophets denounced, "plans to prosper you," the 70 years, M) + **Jeremiah 31** (the New Covenant chapter — restoration, Rachel weeping at Ramah, Ephraim's repentance, "I will write it on their hearts," M) — closes the "Jer 1, 29, 31" roadmap item. Jer 29/31 map thematically with a dashed leg per the established prophetic/non-narrative convention (Pss 22/23/51/137, Isa 6/40/53 precedent). New people `hananiah-prophet`, `ahab-prophet` (disambiguated from King Ahab per the `gad`/`tamar` precedent), `zedekiah-prophet` (disambiguated from King Zedekiah), `shemaiah-nehelamite`; new objects `almond-rod`, `boiling-pot`; no new places (reused `anathoth`/`jerusalem`/`babylon`/`ramah`/`samaria`; `anathoth`/`samaria` topped up with a missing `meaning` field per issue #7's convention). All new ids checked for collisions first — zero duplicates across 147 places / 250 people / 115 objects, independently re-verified by the orchestrator via a standalone Node script (matches the agent's own count). Fixtures `tests/fixtures/24-1.json` (19/19 verses), `24-29.json` (32/32), `24-31.json` (40/40); sweep entries `"Jeremiah 1\|24:1"`, `"Jeremiah 29\|24:29"`, `"Jeremiah 31\|24:31"` added to `CURATED_SWEEP`. `node --check` clean on all 5 touched files; full offline Playwright suite (`PLAYWRIGHT_BROWSERS_PATH=/opt/pw-browsers node tests/smoke.js`) — 786 assertions, PASS, no regressions, independently re-verified by the orchestrator before commit. | 1× sonnet ENG (curation) + orchestrator (issue scan, PR #64 merge, branch restart/push, independent id-collision + test re-verification) | ✅ shipped |
| — | NEXT SESSION | Re-scanned GitHub issues 2026-08-26 (iteration 41): still 0 open, 1 closed total ever filed (#7, already shipped) — nothing new to slot. Jer 1, 29, 31 shipped this session; check for its PR at session start and merge after independent re-verification before starting new work (per the established stale/draft-PR cleanup pattern), unless the user has already merged it. Continue the "Then" roadmap with **Ezek 1, 37** next (the next unchecked item after Jer 1, 29, 31). `node tests/smoke.js` catches data/logic regressions but nothing runs it in CI yet (Tier 3 gate still open; only `deploy.yml`'s post-deploy `verify` job exists, which just polls the live URL, and it only triggers on push to `main`, not on PRs). Note: fresh containers may not have `playwright` pre-installed under the repo — `npm i playwright --no-save` (a few seconds, uses the preinstalled Chromium at `/opt/pw-browsers`) is needed before `node tests/smoke.js` will run. | ≤1 sonnet | pending |

### Curation roadmap (tick as shipped; M = medium depth, L = deep)
**1 Samuel:** [x] 28(L) · [x] 3(M) · [x] 4–6(M) · [x] 8–10(M) · [x] 15(M) · [x] 16(M) · [x] 17(L) · [x] 24(M) · [x] 25(M) · [x] 31(L) — 1 Samuel key-chapter list COMPLETE
**2 Samuel:** [x] 1(M) · [x] 2(M) · [x] 3(M) · [x] 4(M) · [x] 5(M) · [x] 6(M) · [x] 7(M) · [x] 8(M) · [x] 9(M) · [x] 10(M) · [x] 11–12(M) · [x] 13–14(M) · [x] 15(M) · [x] 16–17(M) · [x] 18(M) · [x] 19(M) · [x] 20(M) · [x] 21(M) · [x] 23(M) · [x] 24(M) — FULL BOOK COMPLETE (ch. 22 is a deliberate skip: poetry, near-duplicate of Ps 18, auto pipeline covers it)
**Genesis:** [x] 1–50 — FULL BOOK COMPLETE (iteration 19; chs. 1-3 at slightly deeper M depth from iteration 15, chs. 4-50 at a compressed M depth for cost efficiency)
**Then (order):** [x] Exodus 3(M), [x] 12(M), [x] 14(M), [x] 19(M), [x] 20(M), [x] 32(M) · [x] Joshua 2(M), 6(M) · [x] Judges 4(M), 7(M), 16(M) · [x] Ruth 1–4(M) — FULL BOOK COMPLETE · [x] 1 Kgs 3(M), 8(M), 17(M), 18(M), 19(M) · [x] 2 Kgs 5(M), 18(M), 19(M), 25(M) · [x] Ezra 3(M) · [x] Neh 2(M), 8(M) · [x] Esther 4(M) · [x] Job 1(M), 2(M), 38(M) · [x] Pss 22(M), 23(M), 51(M), 137(M) · [x] Isa 6(M), 40(M), 53(M) · [x] Jer 1(M), 29(M), 31(M) · Ezek 1, 37 · Dan 1–6 · Jonah 1–4 · Mic 5 · Hag 1 ·
**NT:** Matt 2, 5–7, 26–28 · Mark 4–5 · Luke [x] 2(M), 10, 15, 24 · John 1, 3, [x] 4(M), 9, 11, 19–21 · Acts 1–2, 8–9, 16–17, [x] 27(M), 28 · Rom 8 · 1 Cor 15 · Rev 1–3, 21–22
**Datasets:** places 147/200 (no new places from iteration 41 — `anathoth`/`samaria` topped up with `meaning` only) · people 250/120 (target exceeded — target was a floor, not a cap; +4 from iteration 41: hananiah-prophet, ahab-prophet, zedekiah-prophet, shemaiah-nehelamite) · objects 115/60 (target exceeded; +2 from iteration 41: almond-rod, boiling-pot)

### Feature backlog (GitHub issues — check this before picking the next batch)
Re-scan open issues each session start (`list_issues` state=OPEN) and re-slot
new ones here by size/risk, cheapest-and-safest first.

| # | Issue | Scope | Slot |
|---|-------|-------|------|
| [#7](https://github.com/elinxie/Bible-verse-visualizer/issues/7) | Add translations to city names | ✅ **Shipped in iteration 7** (see Ledger); 3 more places (engedi/ziph/maon) topped up with `meaning` in iteration 8; `eden` shipped with `meaning` from birth in iteration 15; all iteration 22 places (gob/zela/abel-beth-maacah) shipped with `meaning` from birth too; `uz` shipped with `meaning` from birth and `edom` topped up in iteration 36. Remaining non-curated places still lack `meaning` — a pure data top-up, safe to ride along with a future curation batch rather than needing its own iteration. | Repo re-scanned 2026-08-26 (iteration 41): still the only issue ever filed (open+closed), already closed — no open issues exist. |

Rule of thumb for future issues: pure data additions (new places/people/objects,
more curated chapters) can ride along with a curation batch; anything that
touches a schema field or the UI (like #7) gets its own feature+tests
iteration so a regression is easy to bisect and revert.
